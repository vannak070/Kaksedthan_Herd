version: '3.8'

services:
  # 1. PostgreSQL Relational Database Service
  db:
    image: postgres:16-alpine
    container_name: kaksedthan_postgres_db
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres123}
      POSTGRES_DB: ${DB_NAME:-kaksedthan_herdbook}
    ports:
      - "5433:5432"
    volumes:
      - kaksedthan_postgres_data:/var/lib/postgresql/data
      - ./src/db/schema.sql:/docker-entrypoint-initdb.d/01_schema.sql
    networks:
      - kaksedthan_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-kaksedthan_herdbook}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s

  # 2. Kaksedthan Backend & Next.js Application Service
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kaksedthan_app_backend
    restart: always
    ports:
      - "3000:3000"
      - "5001:5001"
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: ${PORT:-5001}
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:5001/api/v1}
      NEXT_PUBLIC_LAN_IP: ${NEXT_PUBLIC_LAN_IP:-}
      LAN_IP: ${LAN_IP:-}
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres123}
      DB_NAME: ${DB_NAME:-kaksedthan_herdbook}
      DB_SSL: "false"
      DB_POOL_MIN: 2
      DB_POOL_MAX: 20
      DB_IDLE_TIMEOUT_MS: 30000
      DB_CONN_TIMEOUT_MS: 5000
    depends_on:
      db:
        condition: service_healthy
    networks:
      - kaksedthan_net

networks:
  kaksedthan_net:
    driver: bridge

volumes:
  kaksedthan_postgres_data:
    driver: local
